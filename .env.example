# ================================
# AdtFoods Environment Configuration
# ================================

# ----------------------------------
# Server Configuration
# ----------------------------------
PORT=3500
NODE_ENV=development

# ----------------------------------
# MongoDB Configuration
# ----------------------------------
# Replace with your actual MongoDB connection string
# For MongoDB Atlas: mongodb+srv://<username>:<password>@cluster.mongodb.net/<dbname>
# For Local MongoDB: mongodb://localhost:27017/adtfoods
MONGO_URI=mongodb://localhost:27017/adtfoods

# ----------------------------------
# JWT Authentication
# ----------------------------------
# Secret key for signing JWT tokens - use a strong random string in production
JWT_SECRET=adtfoods-jwt-secret-key-2025

# ----------------------------------
# Redis Cache Configuration (Optional)
# ----------------------------------
# If Redis is not configured, the app will run without caching
# Redis improves performance by caching frequently accessed data

# Option 1: Using Redis URL (recommended for cloud services like Redis Cloud, Heroku Redis)
# REDIS_URL=redis://username:password@hostname:port

# Option 2: Using individual Redis connection parameters (for local or custom setup)
REDIS_HOST=localhost
REDIS_PORT=6379
# REDIS_PASSWORD=your_redis_password  # Uncomment if your Redis instance requires authentication

# ----------------------------------
# Payment Gateway (Razorpay)
# ----------------------------------
# Get your keys from https://dashboard.razorpay.com/app/keys
RAZORPAY_KEY_ID=rzp_test_your_key_id
RAZORPAY_KEY_SECRET=your_key_secret

# ----------------------------------
# Notes
# ----------------------------------
# 1. Never commit this file with real credentials to version control
# 2. Copy this file to .env and fill in your actual values
# 3. Make sure .env is listed in .gitignore
# 4. Redis is optional - the app will work without it but with better performance when enabled
