# MongoDB
MONGO_URI=mongodb://localhost:27017/adtfoods
# or use MongoDB Atlas
# MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/adtfoods

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Razorpay
RAZORPAY_KEY_ID=your_razorpay_key_id
RAZORPAY_KEY_SECRET=your_razorpay_key_secret

# Server
PORT=3500
NODE_ENV=production

# Redis Configuration (Optional for production caching)
# Option 1: Using Redis URL (e.g., from Redis Cloud, Upstash, or Railway)
REDIS_URL=redis://default:password@host:port

# Option 2: Using individual Redis connection parameters
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=your_redis_password

# If Redis is not configured, the app will run without caching
# For local development, you can skip Redis configuration
